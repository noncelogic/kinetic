import type { ReactElement } from 'react';

import { ComingSoon } from '@/components/ui';

export default function AdminPage(): ReactElement {
  return (
    <ComingSoon
      title="Admin"
      description="Team management, API keys, and system configuration — coming soon."
    />
  );
}
