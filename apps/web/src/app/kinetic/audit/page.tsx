import type { ReactElement } from 'react';

import { ComingSoon } from '@/components/ui';

export default function AuditPage(): ReactElement {
  return (
    <ComingSoon
      title="Audit Logs"
      description="Full policy evaluation history and compliance trail — coming soon."
    />
  );
}
